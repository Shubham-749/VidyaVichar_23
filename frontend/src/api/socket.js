import { io } from 'socket.io-client';let socket;export const connectSocket=(token,lectureId)=>{socket=io('http://localhost:5000',{query:{token,lectureId},transports:['websocket']});return socket;};export const getSocket=()=>socket;export const disconnectSocket=()=>{if(socket)socket.disconnect();};